(() => {

const allLinks = Array.from(document.getElementsByClassName("js-folders-user-profile-link"))
let counter = 0

const openLoop = setInterval(() => {
  window.open(allLinks[counter], "popUp", "width=300,height=300")
  counter++
  counter > allLinks.length && clearInterval(openLoop)
}, 100)
})()
